find_package(Qt6 6.8 REQUIRED COMPONENTS Gui)

add_library(astra-render src/ProjectionOutput.cpp src/ProjectionLayer.cpp src/RenderGraph.cpp src/Homography.cpp src/PrivacyMask.cpp src/MeshWarp.cpp src/CropOverscan.cpp src/ColorCompensation.cpp)
target_include_directories(astra-render PUBLIC include)
target_link_libraries(astra-render PUBLIC Qt6::Core Qt6::Gui astra-common)
target_compile_features(astra-render PUBLIC cxx_std_20)
target_compile_options(astra-render PRIVATE -Wall -Wextra -Wpedantic -Werror)

if(BUILD_TESTING)
    add_executable(astra-render-output-tests tests/projection_output_test.cpp)
    target_link_libraries(astra-render-output-tests PRIVATE astra-render)
    add_test(NAME astra-render-output-tests COMMAND astra-render-output-tests)
    add_executable(astra-render-layer-tests tests/projection_layer_test.cpp)
    target_link_libraries(astra-render-layer-tests PRIVATE astra-render)
    add_test(NAME astra-render-layer-tests COMMAND astra-render-layer-tests)
    add_executable(astra-render-graph-tests tests/render_graph_test.cpp)
    target_link_libraries(astra-render-graph-tests PRIVATE astra-render)
    add_test(NAME astra-render-graph-tests COMMAND astra-render-graph-tests)
    add_executable(astra-render-homography-tests tests/homography_test.cpp)
    target_link_libraries(astra-render-homography-tests PRIVATE astra-render)
    add_test(NAME astra-render-homography-tests COMMAND astra-render-homography-tests)
    add_executable(astra-render-privacy-mask-tests tests/privacy_mask_test.cpp)
    target_link_libraries(astra-render-privacy-mask-tests PRIVATE astra-render)
    add_test(NAME astra-render-privacy-mask-tests COMMAND astra-render-privacy-mask-tests)
    add_executable(astra-render-mesh-warp-tests tests/mesh_warp_test.cpp)
    target_link_libraries(astra-render-mesh-warp-tests PRIVATE astra-render)
    add_test(NAME astra-render-mesh-warp-tests COMMAND astra-render-mesh-warp-tests)
    add_executable(astra-render-crop-overscan-tests tests/crop_overscan_test.cpp)
    target_link_libraries(astra-render-crop-overscan-tests PRIVATE astra-render)
    add_test(NAME astra-render-crop-overscan-tests COMMAND astra-render-crop-overscan-tests)
    add_executable(astra-render-color-compensation-tests tests/color_compensation_test.cpp)
    target_link_libraries(astra-render-color-compensation-tests PRIVATE astra-render)
    add_test(NAME astra-render-color-compensation-tests COMMAND astra-render-color-compensation-tests)
endif()
