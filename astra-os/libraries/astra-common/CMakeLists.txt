add_library(astra-common)
target_sources(astra-common PRIVATE src/PrivacyLevel.cpp src/ErrorCode.cpp src/ErrorCodeRegistry.cpp src/Identifiers.cpp src/PlatformPaths.cpp src/CapabilityToken.cpp)
target_include_directories(astra-common PUBLIC include)
target_link_libraries(astra-common PUBLIC Qt6::Core)
target_compile_features(astra-common PUBLIC cxx_std_20)
target_compile_options(astra-common PRIVATE -Wall -Wextra -Wpedantic -Werror)

if(BUILD_TESTING)
    add_executable(astra-error-code-registry-tests tests/error_code_registry_test.cpp)
    target_link_libraries(astra-error-code-registry-tests PRIVATE astra-common Qt6::Core)
    add_test(NAME astra-error-code-registry-tests COMMAND astra-error-code-registry-tests)
    add_executable(astra-common-tests tests/common_domain_types_test.cpp)
    target_link_libraries(astra-common-tests PRIVATE astra-common)
    add_test(NAME astra-common-tests COMMAND astra-common-tests)
    add_executable(astra-platform-paths-tests tests/platform_paths_test.cpp)
    target_link_libraries(astra-platform-paths-tests PRIVATE astra-common)
    add_test(NAME astra-platform-paths-tests COMMAND astra-platform-paths-tests)
endif()
