# P5 Notification System

Notifications carry severity, privacy, target, expiry, action requirements, and
actions. INFO/SUCCESS/WARNING never steal input focus. ERROR may request
attention. CRITICAL uses system focus and blocks ordinary input until handled.
Expiry removes the component and releases focus.
