# P5 Layout Engine

Stack, grid, radial, freeform, and anchor-relative strategies consume the same
constraints and produce bounded `SpatialBounds`. Protected privacy badges are
placed above ordinary components. Constraint failure restores a deterministic
stack layout and reports 5302/5303 rather than emitting unsafe geometry.
