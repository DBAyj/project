# P5 Animation System

The coordinator owns fade, slide, scale, rotate, layout, and focus transitions.
Transitions are non-blocking, cancellable, and end in a deterministic state.
Reduce-motion shortens nonessential motion; disabled animation completes
immediately. Security prompts and privacy hiding never wait for animation.
